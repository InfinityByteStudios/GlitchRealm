{
  "rules": {
    ".read": false,
    ".write": false,
    "leaderboard": {
      ".read": true,
      "$difficulty": {
        ".read": true,
        "$entryId": {
          ".write": "!data.exists() && newData.exists() && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 50 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 100000000 && (!newData.child('survivalTime').exists() || (newData.child('survivalTime').isNumber() && newData.child('survivalTime').val() >= 0)) && newData.child('timestamp').isNumber()"
        }
      }
    }
  }
}
